# tests/unit/egress/

Unit tests mirroring `crates/egress/`. Not yet populated -- Phase 0
scaffolding only; see `tmp/wip/implementation-plan.md` for which phase
adds logic (and therefore tests) to this crate.
