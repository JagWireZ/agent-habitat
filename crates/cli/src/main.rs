//! `habitat` -- the operator-facing CLI entrypoint.
//!
//! Phase 0 scaffolding only: no subcommands, no logic. `habitat install`
//! (Phase 1) and `habitat run` (Phase 7, assembling Phases 1-6) are built
//! against this skeleton in later phases -- see
//! `tmp/wip/implementation-plan.md` and `file-structure.md`.

fn main() {
    eprintln!("habitat: not yet implemented (Phase 0 scaffolding only)");
    std::process::exit(1);
}
