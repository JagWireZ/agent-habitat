[package]
name = "habitat-vm"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "VM launch via rootless Podman + the krun runtime (libkrun), and session lifecycle (Phase 3)."

[dependencies]
